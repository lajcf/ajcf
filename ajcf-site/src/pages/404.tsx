import React from "react";
import Layout from "../components/Shared/layout";

const NotFoundPage = () => (
  <Layout>
    <h1>NOT FOUND</h1>
    <p>Not a valid URL</p>
  </Layout>
);

export default NotFoundPage;
