fragment pressFile on Asset {
  fileName
  url
  createdAt
  optionalPublishedDate
  assetLabel
}

query pressFilesQuery($stage: Stage!) {
  assets(stage: $stage, where: { assetLabel_contains_some: Press }, orderBy: createdAt_DESC) {
    ...pressFile
  }
}
