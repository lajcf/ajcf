fragment eventPage on Event {
  id
  title
  content {
    html
  }
  date
  eventLabels
  cover {
    id
    url
  }
}

query eventPageQuery($id: ID!) {
  event(where: { id: $id }, stage: DRAFT) {
    ...eventPage
  }
}
