import React from "react";
import styles from "./Banner.module.scss";

export const Banner = () => {
  return (
    <section className={styles.banner}>
      <figure>Image</figure>
    </section>
  );
};
